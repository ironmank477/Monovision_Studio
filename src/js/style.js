let sumArr = ["aple", 1, 45];

// sumArr.forEach(function (item, index, array) {
// 	console.log(`${item} Позиция ${index}, в массиве ${array}`);
// })

sumArr.forEach(function (item, index, arr) {
	console.log(`${item} position ${index} in massive ${arr}`);
});


sumArr.forEach(function (item, index, arr) {

})

let b = new Array(10);
console.log(b);
